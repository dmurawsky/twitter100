const postDisconnect = require('../../../server/routes/postDisconnect')

describe('POST /disconnect', function(){
  it('should be disconnected from Twitter and receive a response with my Twitter ID for confirmation')
})
